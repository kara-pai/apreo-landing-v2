import { DashboardContent } from "../dashboard-content";

export const metadata = { title: "APREO | Provider Dashboard" };

export default function ProviderDashboard() {
  return <DashboardContent role="provider" />;
}
